# Lead capture extension

Load this folder as an unpacked extension in a Chromium browser. The popup sends the active page URL to the hosted app as `submit_url` and `submit_title`.

The hosted app requires a signed-in Supabase user before it can insert the lead into `review_queue`.
